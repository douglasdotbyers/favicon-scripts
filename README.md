# Favicon Scripts

Script to generate favicons (`.ico` / `.png`) from source `favicon.svg`.

## Setup

Ensure *ImageMagick* is installed:

```bash
convert --version
```

## Usage

Run script:

```bash
./make.sh
```
