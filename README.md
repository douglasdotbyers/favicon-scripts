# Favicon Scripts

Script to generate `.ico` / `.png` favicons from source `favicon.svg`.

## Setup

Ensure `convert` (ImageMagick) is installed:

```bash
convert --version
```

## Usage

Run script:

```bash
./make.sh
```
